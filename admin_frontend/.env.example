# Console 要打的 API 位址（決策 D1）。
#
# ⚠️ 本檔只是範本。真正的 .env / .env.* 被 .gitignore 的機密規則擋住（刻意的，不要開洞）——
#    正式的 build／部署一律由 deploy.sh 以 shell 環境變數注入，那裡是唯一的真實來源。
#    Vite 會直接讀 shell 上 VITE_ 開頭的變數，不需要 .env 檔也能烘進 bundle（已實測）。
#
# 本機開發（npm run dev）才需要複製本檔為 .env.local 並填值：
#
#   cp .env.example .env.local
#
# 結尾**必須帶 stage 路徑**（例：/stg）—— api.ts 是 BASE_URL + '/admin/...' 串接的，
# 漏掉會整片 403。結尾不要加斜線（api.ts 會幫忙去掉，但別依賴）。
#
# 沒設值時 Console 會在載入時直接 throw，不會退回任何預設位址 —— 這是刻意的 fail-closed，
# 避免產出「看起來正常、實際打到工程師 prod」的 bundle。

VITE_API_BASE_URL=https://9mu0vajn38.execute-api.ap-southeast-1.amazonaws.com/stg
