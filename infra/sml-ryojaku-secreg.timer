[Unit]
Description=両雀 安全回歸守衛排程（每日 10:15 台北）

[Timer]
# 為什麼是**每日**：這支打的是**線上 stg**，而「線上是不是修好的那一版」會被
# 任何一次部署改變。每日跑的意思是「最壞情況下 24 小時內會有人被告知」。
#
# 為什麼**不是每小時**：底層腳本每次註冊 2 個帳號，而 app-register 的限流是
# 每 IP 每小時 10 次 ⇒ 本機每小時最多 5 次。排程佔太密的話，人想手動跑一次
# 就會撞限流，而撞限流的輸出（前置失敗）跟「守衛壞了」長得很像。留餘裕給人。
#
# 為什麼是 **10:15 台北**（＝02:15 UTC）：偵測與時刻無關（回歸是持續狀態），
# 所以這個時刻是挑給**人**的 —— 告警落在台北早上，當天讀得到。排半夜的話沒人看。
# 09:30 是 sml-tianwang-audit、09:45 是 sml-geo-verify，錯開 30 分鐘不互相擠。
OnCalendar=*-*-* 10:15:00 Asia/Taipei
# 機器沒開機時錯過的排程，開機後補跑：「stg 回歸了」是持續狀態，補跑照樣抓得到。
Persistent=true
AccuracySec=1min

[Install]
WantedBy=timers.target
